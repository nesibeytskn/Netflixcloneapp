import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const LightContentText = ({content}) => {
  return <Text className="text-gray-400">{content}</Text>;
};

export default LightContentText;

const styles = StyleSheet.create({});
